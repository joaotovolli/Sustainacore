# Make `app` an explicit Python package.
#
# This avoids ambiguity with the top-level `app.py` module during imports/tests
# and matches how the retrieval service is referenced (e.g. `app.retrieval.app`).

