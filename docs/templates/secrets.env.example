# Placeholders only. Do not commit real values.

# VM1 API auth (shared with VM2 BACKEND_API_TOKEN)
API_AUTH_TOKEN=<API_AUTH_TOKEN>
AUTH_TOKEN_SIGNING_KEY=<AUTH_TOKEN_SIGNING_KEY>

# Market data provider (SC_IDX)
MARKET_DATA_API_BASE_URL=<MARKET_DATA_API_BASE_URL>
MARKET_DATA_API_KEY=<MARKET_DATA_API_KEY>
SC_MARKET_DATA_API_KEY=<SC_MARKET_DATA_API_KEY>

# Oracle wallet usage (typically via /etc/sustainacore/db.env)
TNS_ADMIN=<TNS_ADMIN>

# Email/SMPP (telemetry + alerts)
SMTP_HOST=<SMTP_HOST>
SMTP_PORT=<SMTP_PORT>
SMTP_USER=<SMTP_USER>
SMTP_PASS=<SMTP_PASS>
MAIL_FROM=<MAIL_FROM>
MAIL_TO=<MAIL_TO>

